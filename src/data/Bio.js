class Bio {
  constructor(
    firstName,
    surName,
    body,
    image,
    links
  ) {
    this.firstName = firstName;
    this.surName = surName;
    this.body = body;
    this.image = image;
    this.links = links;
  }
}

export default Bio;
