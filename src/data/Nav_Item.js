class Nav_Item {
  constructor(
    title,
    className,
    link,
    subLinks
  ) {
    this.title = title;
    this.className = className;
    this.link = link;
    this.subLinks = subLinks;
  }
}

export default Nav_Item;
