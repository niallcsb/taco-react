class Nav_Item {
  constructor(
    title,
    className,
    link,
    subLinks,
    key
  ) {
    this.title = title;
    this.className = className;
    this.link = link;
    this.subLinks = subLinks;
    this.key = key;
  }
}

export default Nav_Item;
